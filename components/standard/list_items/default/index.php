<?

if (isset($result))
    if (count($result) > 0) {
        ?>

        <? preprint($result); ?>

        <?

        foreach ($result as $item) {
            
        };
        ?>

    <? };
?>